[build]
builder = "nixpacks"
# Installation reproductible + génération du client Prisma + application des migrations.
# Nécessite que DATABASE_URL soit défini dans les variables Railway avant le build.
buildCommand = "npm ci && npx prisma generate && npx prisma migrate deploy"

[deploy]
startCommand = "node index.js"
healthcheckPath = "/api/health"
